Installation
===================

``pypi_downloads`` is currently hosted on PyPI. It required python >= 3.8.

You can simply install pypi_downloads from PyPI with the following command:

.. code:: shell

    pip install pypi_downloads

You can also install with the newest version through GitHub:

.. code:: shell

    pip install -U git+https://github.com/hansbug/pypi_downloads.git@main

After installation, run this python code, and version information \
of ``pypi_downloads`` should be shown.

.. literalinclude:: install_check.demo.py
    :language: python
    :linenos:

.. literalinclude:: install_check.demo.py.txt
    :language: text
    :linenos:

pypi_downloads is still under development, you can also check out the documents in stable version through `https://hansbug.github.io/pypi_downloads/ <https://hansbug.github.io/pypi_downloads/>`_.
